{
  "name": "scorativenotify",
  "version": "0.1.1",
  "description": "Notifies you when a room is open in socrative, so you don't need to be present in class",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "log4js": "^6.6.1",
    "twilio": "^3.11.3"
  }
}
